# Core serving stack. Installed on the GPU host; the pure-Python core and the
# `mock` backend need none of this beyond PyYAML.

# --- always needed ---
pyyaml>=6.0
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27
prometheus-client>=0.20

# --- vLLM backend (CUDA 12.x, NVIDIA GPU required) ---
# vllm>=0.6.0
# torch>=2.4
# transformers>=4.44
# peft>=0.12          # QLoRA adapter loading
# bitsandbytes>=0.43

# --- Triton Inference Server client ---
# tritonclient[all]>=2.48

# --- Ray Serve ---
# ray[serve]>=2.35

# --- TensorRT-LLM engine export (use the NGC container; pip install is fragile) ---
# tensorrt-llm>=0.12
# modelopt>=0.15

# --- benchmarking / reporting ---
# nvidia-ml-py>=12.555     # GPU utilization sampling via NVML
